# Rust build artifacts
target/

# Compiled WASM output (tracked separately in releases, not in source)
*.wasm

# Cargo lock — committed to freeze transitive dependency versions
# for reproducible CI builds with soroban-sdk 22.0.7

# Editor and IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# macOS metadata
.DS_Store

# Environment files — never commit secrets
.env
.env.*
!.env.example

# Coverage reports
coverage/
lcov.info

# Temporary files
*.tmp
*.bak
